import { EditarDirective } from './editar.directive';

describe('EditarDirective', () => {
  it('should create an instance', () => {
    const directive = new EditarDirective();
    expect(directive).toBeTruthy();
  });
});
